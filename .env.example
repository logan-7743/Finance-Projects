# Root-level example only. Individual apps may also have their own .env.example files.

# Frontend uses this to call the local FastAPI backend.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Backend CORS.
ALLOWED_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]
